# mychat-svelte Project Memory

> **Single source of truth is `/CLAUDE.md` at the repository root.**
> All project context, stack details, workflow preferences, and doc references live there.
> This file is intentionally empty to avoid duplication and drift.
